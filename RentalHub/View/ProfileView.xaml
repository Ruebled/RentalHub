<UserControl x:Class="RentalHub.View.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RentalHub.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="User Profile" FontSize="24" FontWeight="Bold" Margin="20"/>

        <!-- User Information -->
        <StackPanel Grid.Row="1" Margin="20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Username:" FontWeight="Bold" Width="100"/>
                <TextBox Text="{Binding UserModel.Username, Mode=TwoWay}" IsReadOnly="{Binding IsEditing, Converter={StaticResource InverseBooleanConverter}}" Margin="0,0,0,10"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Email:" FontWeight="Bold" Width="100"/>
                <TextBox Text="{Binding UserModel.Email, Mode=TwoWay}" IsReadOnly="{Binding IsEditing, Converter={StaticResource InverseBooleanConverter}}" Margin="0,0,0,10"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Full Name:" FontWeight="Bold" Width="100"/>
                <TextBox Text="{Binding UserModel.FullName, Mode=TwoWay}" IsReadOnly="{Binding IsEditing, Converter={StaticResource InverseBooleanConverter}}" Margin="0,0,0,10"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Phone Number:" FontWeight="Bold" Width="100"/>
                <TextBox Text="{Binding UserModel.PhoneNumber, Mode=TwoWay}" IsReadOnly="{Binding IsEditing, Converter={StaticResource InverseBooleanConverter}}" Margin="0,0,0,10"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="User Type:" FontWeight="Bold" Width="100"/>
                <TextBox Text="{Binding UserModel.UserType, Mode=TwoWay}" IsReadOnly="{Binding IsEditing, Converter={StaticResource InverseBooleanConverter}}" Margin="0,0,0,10"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Registration Date:" FontWeight="Bold" Width="100"/>
                <TextBox Text="{Binding UserModel.CreateDate, Mode=TwoWay}" IsReadOnly="True" Margin="0,0,0,10"/>
            </StackPanel>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="20,0,0,20">
            <Button Content="Edit" Command="{Binding EditCommand}" Width="100"/>
            <Button Content="Save" Command="{Binding SaveCommand}" Width="100" Margin="10,0,0,0" Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100" Margin="10,0,0,0" Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>
    </Grid>
</UserControl>
